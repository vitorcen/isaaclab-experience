---
name: sonic-wbc-vla-route
description: 架构侧 GR00T×SONIC-WBC 路线 + 路 A 动作源（本地 deploy demo → robot_filtered）的可执行计划与评审验过的坑
metadata:
  type: project
---

> 🗂️ **2026-06-08 拆成独立 sub 仓库 `LeSONIC`**（`git@github.com:vitorcen/LeSONIC.git`，作 isaaclab-experience 的 submodule；理念=isaaclab 偏文档引用成果、分权好维护）。结构=**完全自包含**：`LeSONIC/{scripts,patches/{gear-sonic,gr00t-n17},doc,SONIC.ipynb(置根),datasets,outputs}` + **两个嵌套 submodule** `LeSONIC/dependencies/{Isaac-GR00T(NVIDIA,N1.7),GR00T-WholeBodyControl(NVlabs,WBC)}`。**Isaac-GR00T 是 LeSONIC 自有副本(cp 含 .venv)，与父 PickOrange 的那份互不干扰**；WBC 整个移入(父不再有)。脚本 `$REPO_ROOT=LeSONIC根(scripts/..)` 自解析,依赖/数据/产物全在内。⚙️ **迁移手法(复用,零重下)**：submodule 的 `.git` 指针+`core.worktree` 都是相对(`../../.git/modules/dependencies/X` / `../../../../dependencies/X`)→ 复刻 `<root>/dependencies/X`↔`<root>/.git/modules/dependencies/X` 结构后 mv/cp **worktree+metadata 两处**,指针自动成立、无需改写;再写 `.gitmodules`+`git add <path>`(stage 成 160000 gitlink)。`.venv`/datasets/outputs gitignore,不入 git。clone 要 `--recursive` 两层。

**目标（架构侧，区别于数据侧 DART 蒸馏 [[mimickit-to-vla-distill-plan]]）**：GR00T N1.7 VLA 只输出
SONIC WBC 的 64 维 FSQ 潜 token，WBC（`sonic_release/last.pt`）当现成平衡底座，按 prompt 出舞蹈/武术。
设计文档：`LeSONIC/doc/groot_sonic_wbc_route.html`（架构 Stage A/B/C）+ `LeSONIC/doc/sonic_dance_motion_source.html`（动作源三路对比）。
2026-06-03 经 codex gpt-5.5 + mimo 两轮独立评审（见 [[feedback-mimo-independent-review]]），结论：**先走路 A**。

## 下一步 = 路 A（也是架构侧 Stage A / Milestone 0 去风险实验）
把本地 SONIC deploy demo 动作转成 Isaac-eval 的 `robot_filtered`，跑 eval 看 **WBC 跟不跟得住踢/舞而不摔**。
```bash
cd dependencies/GR00T-WholeBodyControl
# ⚠️ --fps 必须 50（eval target_fps=50），转换器默认 30；先确认 deploy CSV 源帧率，源≠50 用 --fps_source
python gear_sonic/data_process/convert_soma_csv_to_motion_lib.py \
    --input gear_sonic_deploy/reference/example/neutral_kick_R_001__A543 \
    --output data/kick_robot.pkl --fps 50
python gear_sonic/eval_agent_trl.py +checkpoint=sonic_release/last.pt +headless=False ++num_envs=1 \
    "++manager_env.commands.motion.motion_lib_cfg.motion_file=data/kick_robot.pkl" \
    "++manager_env.commands.motion.motion_lib_cfg.smpl_motion_file=dummy"
```
启动方式同 [[gear-sonic-preview-setup]]（isaaclab env、preview.sh 同款 hydra override）。

## 评审验过的关键事实（别再踩）
- **动作源**：`gear_sonic_deploy/reference/example/` 本地已有 **13 条**（含 `_M`）：macarena/dance_in_da_party（舞）、
  neutral_kick（踢）、lunge、one_leg_jumping、squat、walking_quip_360。CSV(joint_pos/body_pos/body_quat)。
- **fps 是唯一真坑**：converter 默认 `--fps 30`，必须传 `50`；源帧率不符会动作变速变形。smpl_joints **不是**坑。
- **smpl 可 dummy**：`smpl_motion_file=dummy`（`motion_lib_base.py:252`）；converter `:301` 已输出 `smpl_joints=zeros(T,24,3)`；
  g1 encoder 只吃 `command_multi_future_*`+`motion_anchor_ori`（robot FK 派生），**根本不读 smpl_joints**（mimo 追源码证）。
- **robot_filtered schema**：`{motion_key:{root_trans_offset(T,3), pose_aa(T,30,3), dof(T,29), root_rot(T,4 xyzw), smpl_joints(T,24,3), fps}}`，joblib(zlib)。
- **转换器** `gear_sonic/data_process/convert_soma_csv_to_motion_lib.py` 输入支持 CSV 目录 / deploy PKL，输出正是上面 schema；内部 `MJ_TO_IL` 处理关节重排（建议跑一次 A/B sanity）。
- **HF `nvidia/GEAR-SONIC` 没有多动作 robot_filtered**（只 walk）；30G `bones_seed_smpl` 是 SMPL human data，不是 robot_filtered 必经前置 → **别为拿动作下 30G**。
- **降调**：deploy demo 可能精选"能跑"的 → 路 A 全过是 **go/no-go 信号**，不等于"覆盖任意武术"。

## 扩容优先级（评审一致）
路 A（13 条 demo，验架构）→ **Bones-SEED 的 G1 retargeted CSV 子集**（关键词筛 dance/kick/jump，同转换器，非 30G SMPL）→ LAFAN→robot（有物理 gap，最后）→ 全量 30G SMPL。

## T1 选定（2026-06-03）：合成 token 数据集 → finetune GR00T → Isaac 内解码（绕开 VR+C++）
路 A 已跑通（7 条 demo SONIC 跟踪 OK，`LeSONIC/scripts/gear_sonic_demo.sh`）。下一步 = **T1 第一步：建 `UNITREE_G1_SONIC` token 数据集**（用户 2026-06-03 选定）。
官方端到端 workflow 实锤（repo `docs/source/tutorials/vla_workflow.md`+`vla_inference.md`+`gear_sonic/scripts/run_vla_inference.py`）：
- **设计正是架构侧**：prompt→GR00T N1.7(2.5Hz)→`action.motion_token`(64)+左右手各7=78维→SONIC decoder(50Hz)→关节；prompt 运行时 `t <text>` 改。
- 🔴 **无现成 G1-SONIC VLA ckpt**，base N1.7-3B 吐不出合法 token，**必须自己 finetune**；官方 deploy 走 ZMQ+C++（连 `--sim` 都要 C++ build）→ 所以走 T1 替代。

### 建数据集的验证过的接口（都读源码确认）
- **写库直接复用官方 `gear_sonic/data/exporter.py:Gr00tDataExporter`**（继承 LeRobotDataset）：`.create(save_root,fps=50,task,features,...)` + `.add_frame(frame_dict)` + `.save_episode()`。schema/modality 用 `gear_sonic/data/features_sonic_vla.py:get_features_sonic_vla / get_modality_config_sonic_vla(robot_model)`。
- **action 关键字段**：`action.motion_token` shape(64) float64（其余 smpl_*/planner_*/vr_* 是 teleop 遥测，盲跳舞可填 0/省）；video=`observation.images.ego_view`(480×640)；prompt 进 `annotation.human.task_description`(=add_frame 的 `frame["task"]`)。
- **token 抽取点（零侵入）**：eval 每步调 `cb.eval_step(env,results)` 且循环给 `callback.model=model` → 写个 **recorder eval-callback** 即可拿 `model.policy.actor_module`(UniversalTokenModule)。Hook 住 `actor_module.decode(name,decode_input_dict)`，入参 `decode_input_dict["token_flattened"]` 就是 64 维 motion_token（= 官方 `proprio["token_state"]`）。GR00T 训练目标若要 pre-quant 连续 latent 用 `actor_module.encode(name,obs,no_quantization=True)`。
- **相机**：env cfg 自带，`++enable_cameras=True`（或 `render_ego=True`）建 `self.ego_camera`(TiledCamera, prim `{ENV_REGEX_NS}/Robot/ego_camera`，默认[108,192]可调)，recorder 读 `env.scene["ego_camera"].data.output["rgb"]`。**不用 dummy 帧**。
- **lerobot 写库 segfault 坑**（[[lerobot-v040-convert-segfault-fix]]）：多 ep 写 LeRobotDataset 随机崩=dual-ffmpeg 堆损坏；若复现，encode 改 ffmpeg-CLI 子进程。先小样本（1 motion×few step headless）smoke 验证再放量。
- 注入用 eval-callback（注册进 `config.callbacks` + `eval_callbacks`），别改 eval 主循环；启动复用 `gear_sonic_demo.sh` 同款 hydra override。

### T1-step1 recorder 已跑通（2026-06-03，验证过）
- **产物**：`gear_sonic/data/vla_token_recorder.py`（submodule，eval-callback）+ `LeSONIC/scripts/gear_sonic_record.sh`（headless 逐条录）。裸 npz 出在 `datasets/sonic_vla_raw/<motion_key>/episode_NNN.npz`。
- **npz schema**：`motion_token(T,64)f32` + `joint_pos(T,29)f32` + `root_quat(T,4)wxyz` + `ego_rgb(T,480,640,3)u8` + `fps=50` + `prompt` + `motion_key`。
- **token 验证实锤**：值落 **1/16 量化网格** `[-0.5625,0.5]`（FSQ levels=16），逐帧变化 → 确是 SONIC 真 token。抽取 = hook `actor_module.decode("g1_dyn",d)` 取 `d["token_flattened"]`，reshape(-1)→64。
- **启动 override**（都在启动器里）：`+headless=True ++num_envs=1 ++manager_env.config.enable_cameras=True "++manager_env.config.cameras.camera_resolution=[480,640]" ++manager_env.commands.motion.{use_paired_motions=True,motion_lib_cfg.{motion_file=...pkl,filter_motion_keys=<key>,smpl_motion_file=dummy}} "++eval_callbacks=[vla_recorder]" ++callbacks.vla_recorder.{_target_=gear_sonic.data.vla_token_recorder.VlaTokenRecorder,output_dir,motion_key,prompt,max_steps,episode_tag}`。
- **踩过的坑**：① callback 必须有 `on_step_end(*a,**k)`（eval 循环对所有 cb 调；顺便从 kwargs 收 model）；② `enable_cameras`/`cameras` 在 **`manager_env.config.*`** 命名空间不是顶层（`env_config=config.manager_env`）；③ token 多一维要 reshape；④ eval 循环结尾自带 `os._exit(0)`，teardown 不卡。
- **ego 相机看地面看不到自己**（attach 到 pelvis 朝前）→ **符合部署现实**（真机相机看世界），盲跳舞视觉本就弱，保留 ego 不要第三人称（否则 train/deploy 失配）。
- **diversity**：env 自带 startup DR（physics_material/joint_default_pos/base_com/body_mass randomize），每次 fresh boot 不同 → 跑 N episodes（N 进程）天然有 DR 多样性；RSI 可加 `sample_from_n_initial_frames`。

### T1-step1 converter 已跑通（2026-06-03，全链路打通）
**SONIC eval → 抽 token → npz → convert → GR00t LeRobot v2.1 数据集**，端到端验证过。
- **走法 A 落地**：`bash install_scripts/install_data_collection.sh` 装好 `.venv_data_collection`（uv,py3.10,lerobot **0.1.0**=老 `lerobot.common` 布局 + gear_sonic + PyAV/OpenCV）。converter = `LeSONIC/scripts/sonic_vla_npz_to_lerobot.py`，**在 `.venv_data_collection` + cwd=WBC 目录跑**（gear_sonic 资产路径相对 WBC）。
- 用官方 `Gr00tDataExporter.create(...).add_frame(frame).save_episode()`，schema=`features_sonic_vla`。**遍历 features 把没采的 teleop 字段零填**保 `validate_frame` 过；实填 motion_token/observation.state/action.wbc/root_orientation/ego_view。
- **num_joints=43**（含手指）但录的 joint_pos 是 29 → `_fit` 零填 29→43（observation.state 后 14 维是零，盲跳舞无碍；训练目标是 motion_token 不是关节）。
- **产物** `datasets/sonic_vla_lerobot/`：codebase_version **v2.1**、fps50、7 ep/3815 帧、`meta/modality.json`(action.motion_token[0:64])、tasks.jsonl 7 prompt、ego_view mp4。load 回测 motion_token(64,)[-0.5,0.44] + ego_view(3,480,640) 全过。**3.2M**（mp4 压地面视角极小，裸 npz 584M）。无 segfault。
- **下一步**：① 放量 `EPISODES=N bash LeSONIC/scripts/gear_sonic_record.sh`(DR 多样性)凑 50-100 demo/任务 → 重跑 converter；② 进 Stage C：Isaac-GR00T `launch_finetune.py --embodiment-tag UNITREE_G1_SONIC` finetune → in-process decode 部署。

### Stage C finetune 已跑通配方（2026-06-03，单 4090）
**全部前置已就位**：`dependencies/Isaac-GR00T` = N1.7（有 `gr00t_n1d7` + `.venv` torch2.7.1+cu128/cudnn90701 干净栈）；`unitree_g1_sonic` embodiment **已注册**（`gr00t/configs/data/embodiment_configs.py:67`，action=[motion_token,左右手joints] horizon40 ABSOLUTE，video=ego_view，lang=task_description）；base **N1.7-3B(6.5G)+Cosmos-Reason2-2B 都在 HF cache**（无 gated 阻塞）。
- 启动器 `LeSONIC/scripts/gr00t_sonic_finetune.sh`（`SMOKE=1` 2步验证 / `MAX_STEPS=N`）。冻结 VLM、训 DiT头+projector（**可训练 1.62B/51.5%**）。
- **两个 skill[gr00t-4090-finetune] 没覆盖的本 launcher 特定坑（OOM 根因）**：① `launch_finetune.py:96` **硬编 `optim="adamw_torch"`**（Adam 动量 state→24G OOM @22.7G），FinetuneConfig 不暴露 optim/grad_ckpt → 改成读 `GR00T_OPTIM`(默认仍 adamw 不破坏) + `GR00T_GRAD_CKPT`；② `experiment.py:227` 的 TrainingArguments **缺 `gradient_checkpointing_kwargs`** → 加 `{"use_reentrant":False}`（冻结 VLM+训 DiT 时 reentrant=True 报无梯度）。**这两处是 Isaac-GR00T 改动，要转 patch**。
- 修后 `GR00T_OPTIM=adafactor GR00T_GRAD_CKPT=1` + global_batch=4/grad_accum=4/1GPU(micro=1) → 峰值 **22688 MiB 不 OOM**，2 步 smoke loss 1.18 exit0。skill 4 个 env flag(COMPILE_ACTION_HEAD_DISABLE/DATALOADER_NUM_WORKERS=0/PIPELINE_OVERLAP_DISABLE=1/expandable_segments)照搬。
- **embodiment 已设 rep=ABSOLUTE → launcher 硬编 `use_relative_action=True` 不触发双重相对化**（只 rep==RELATIVE 才触发），motion_token 安全。
- derisk 目标：2000 步看 train_loss 降不降（能降=GR00T 学得会 (ego,prompt)→token，go；plateau=查视觉瓶颈/token回归）。

### Stage C derisk 结论（2026-06-03）：✅ 学得会，欠训，方向 GO
2000 步跑完 exit0，train_loss 1.18→0.50 平滑单调降（DiT diffusion loss，绝对值不可直接判，0.5 平台是噪声底正常）。
**真判据 = `gr00t/eval/open_loop_eval.py`（预测 vs GT 动作 MSE）**，全 7 trajectory（`--action_horizon 40 --embodiment_tag unitree_g1_sonic --traj_ids 0..6`）：
- checkpoint-1000 平均 token MSE **0.0875** → checkpoint-2000 **0.0650**（**单调降 ~25%/1000步**）。
- 基线：predict-global-mean **0.048**、predict-per-motion-mean **0.039**（=各动作 token 序列方差，脚本现算）。7 动作 mean-token L1 距离 **0.114** → token 空间有真信号可分。
- **判读**：2000 步 MSE 仍高于基线（欠训，loss/MSE 都还在降），但**单调改善 = 在学习轨道上，不是根本不行**。结论 **GO**：加步数（skill 推荐 8k–20k）+ 后续多样性（RSI/更多 clip）应能压到基线下。
- 开环 eval 廉价好用，比 loss 值可信；MSE 含 14 维零手部会拉低一点，token-only 实际略高。denoising_steps=4 默认。
- ~~下一步 8000 步重训~~ **作废**：见下方闭环诊断，先别加步数。

### in-process decode 部署 + 闭环诊断（2026-06-03，codex 评审驱动）
建了 token 注入器 `gear_sonic/data/vla_token_injector.py`（hook `actor_module.decode` **替换** `token_flattened` 为外部 token）+ `LeSONIC/scripts/gear_sonic_inject.sh` + dumper `LeSONIC/scripts/gr00t_dump_pred_tokens.py`（GR00T venv 跑 open-loop 出每动作预测 token → `datasets/sonic_vla_pred/<key>.npz`）。**prompt→GR00T→token→SONIC decode→G1 在 Isaac viewer 全内存跑通，无 C++/ZMQ。**
- **codex gpt-5.5 评审推翻"GO/加步数"**：硬伤=每动作1ep无held-out（只能背训练集/退化成prompt均值模板）、**相位观测缺失**（ego看地面+prompt静态+state可能错位+gravity零→40-step chunk相位无源→开环漂）、**连续回归量化FSQ token是hack**（应预测pre-quant latent过SONIC quantizer，或当16-level分类）、LR@2000已近0 loss在0.5抖→0.065可能就是平台。codex判：derisk**还不能证明架构侧GO**，连per-motion-mean基线都没打过。
- **codex 的决定性实验 = 3路闭环token回放**，已跑：① 预测token注入 → G1 每**1-2s重置**；② **GT token（我录的真token）注入也每1-2s重置** → 不是模型问题。③ 根因=`base_adaptive_strict_ori_foot_xyz` 终止严（anchor_pos 0.15m/ee 0.15/foot 0.2/ori 0.2），**开环注入无actor闭环纠偏→漂移>阈值→终止**。④ **放宽终止**（`++manager_env.terminations.{anchor_pos,ee_body_pos,anchor_ori_full,foot_pos_xyz}.params.threshold=99`）→ **GT token 开环能完整复现踢腿** → **注入接口/架构层通了**，1-2s重置是终止严格度+开环的confound不是接口坏。
- **最终对照结果（放宽终止）**：GT token → 完整踢腿；**predicted token(旧2000) → 出"微踢腿"轮廓（方向对、幅度被压扁）**。

### 决定性翻案（2026-06-03 晚）：MSE 暴跌，架构侧 GO 坐实，之前是 LR-schedule 欠训
旧 2000-run loss 卡 0.50 是 **max_steps=2000 的 LR schedule 衰减到 0** 不是天花板（codex 对 LR 判断正确）。**全新 max_steps=8000 跑（让 LR 铺满）**：loss 1.18→**0.10**（step~2400），同样 step 2000 的 checkpoint open-loop MSE：
- **平均 0.0167**（旧 2000-run 是 0.065）；逐动作 dance0.019/lunge0.010/macarena0.033/kick0.023/squat0.009/jump0.011/walk0.010。
- **比 per-motion-mean 基线(0.039)低 2.3×、比 global-mean(0.048)低 2.9×** → 模型**确实学会 (ego,prompt)→token**，不是背均值。之前"高于基线"纯 LR 欠训假象。
- **架构侧 GO 确认**（接口通 + 模型真学会 + 远破基线）。caveat：仍是 7-ep 训练集，泛化(held-out)未测；macarena/kick 最难但仍破基线。
- **8k run 在 step 2429 崩**（`RuntimeError: d.is_cuda() INTERNAL ASSERT FAILED` backward 时偶发 CUDA 故障；skill gotcha#3，干净 env 也偶发）→ resume from checkpoint-2000（`trainer.train(resume_from_checkpoint=True)` 自动找最新 ckpt；SAVE_STEPS=500 最多丢 500 步）。
- 新预测 token dump 在 `datasets/sonic_vla_pred_8k/`；放宽终止注入看清晰版踢腿。

### 8k 训练完成（2026-06-03 晚，含死机恢复）：MSE 0.0011，架构 GO 强确认
- **train_loss 1.18→~0.04**（8000步）。**open-loop MSE 随步数单调暴跌：旧2000-run 0.065 → 8k@2000 0.0167 → 8k@8000 0.0011**（比 per-motion-mean 基线 0.039 低 **35×**）。逐动作 dance0.0010/lunge0.0026/macarena0.0003/kick0.0018/squat0.0009/jump0.0005/walk0.0006。kick 0.023→0.0018(13×) → 踢腿从"跨步"变利落。
- **结论坐实**：GR00T 能把 (ego,prompt)→SONIC token 学到极紧（7-ep 训练集过拟合）。之前所有"糊/跨步/高MSE"全是 **LR-schedule 欠训**，非建模天花板。codex 的"continuous回归抹平"担忧在足够训练下被克服（至少训练集内）。**caveat 仍在：泛化(held-out新prompt/新clip)未测**——这才是下一个真问题。
- **死机恢复经验（reboot mid-save 损坏）**：① checkpoint 写一半被 reboot 截断 → safetensors `incomplete metadata` 损坏（dir 变小，如 8.4G vs 完整 12G），resume 死循环卡它；② 修=删损坏 ckpt 回退到上一个完整的；③ **自愈 resume loop** `/tmp/resume_8k_heal.sh`：每次 attempt 前用 `/tmp/ckpt_intact.py` 校验所有 ckpt，损坏即删，再 resume latest intact + crash 自动续。
- **checkpoint 双层保留**（用户偏好 [[feedback-training-save-policy]]）：`SAVE_LIMIT=3` 滚动临时（崩恢复）+ **阶段点 hardlink 到 `outputs/.../keep/`**（`/tmp/keep_stage_ckpts.sh`，2000倍数，`cp -al` 同 inode 零额外磁盘，HF 滚删原件 inode 仍由 keep/ 保住）。keep/ 存 2000/4000/6000/8000。每 ckpt 12G。
- **下一步（真问题=泛化）**：① 拿 held-out（新 episode/新 phase/改写 prompt）测 MSE 看是否仍低（过拟合 vs 真泛化）；② 多 clip + RSI 扩数据；③ 闭环（非放宽终止）下 predicted token 的 tracking 误差/相位漂。脚手架：`LeSONIC/scripts/gr00t_sonic_finetune.sh`(SAVE_LIMIT/GR00T_OPTIM/GR00T_GRAD_CKPT env)+ `gr00t_dump_pred_tokens.py` + `gear_sonic_inject.sh` + open_loop_eval。
- **下一步（codex 序）**：先别 8k。① 修 observation.state joint mapping + 补真 projected_gravity；② predicted-token 放宽终止闭环 vs GT vs per-motion-mean 对照（看动作质量/相位漂）；③ 若预测明显劣于 mean → 改 token 目标形式（分类/pre-quant latent）+ 加 held-out 多clip，再谈步数。

### 部署/可视化基建 + 一键化（2026-06-03 晚，compact 续接点）
- **注入器** `gear_sonic/data/vla_token_injector.py`(submodule)：hook `actor_module.decode` 替换 `token_flattened` 为外部 token。支持 **per-env 多 token**（`token_npz` 逗号分隔，env i 用 list[i]）→ 老师/学生并排。`tint_env0_green` 选项**已废**（见下）。
- **一键脚本**：`LeSONIC/scripts/gear_sonic_inject.sh <motion>`（默认 `datasets/sonic_vla_pred_8k_final/` 的 8000-token + `RELAX=1` 放宽终止 + `debug_vis=True`）。预测 token 在 `datasets/sonic_vla_pred_8k_final/`（7 条全有，checkpoint-8000 dump）。
- **多-env 并排对比配方（codex 验证，重要）**：单视图只见 1 个机器人 = 相机自动跟随 **env_0**（`manager_env_wrapper.py:169 update_view_to_asset_root`）+ 默认 `eye=[4.5,0,4]` 从 +X 看，env_1 在后方。修法：`++num_envs=2 ++manager_env.config.terrain_type=plane ++manager_env.config.env_spacing=3.0 '++manager_env.config.viewer.eye=[0.0,-7.0,3.0]' '++manager_env.config.viewer.lookat=[0.0,0.0,1.0]'`。⚠️ `env_spacing` 在 **`manager_env.config.env_spacing`** 不是顶层；hydra 逗号 list 值（eye/token_npz 多路径）要 **单引号**。
- **🔴 机器人本体逐 env 上色 = 死路**：robot 是 instanceable 资产，mesh 是只读 instance proxy；`SetInstanceable(False)` 去实例化能绑色(bound 50 meshes)但**破坏物理 articulation**（"Failed to set DOF actuation forces"）。结论：用左右**位置**区分老师/学生，别给本体上色；真要参考可视化用 `goal_pos_visualizer` 绿球 marker（独立 prim 不碰物理，`debug_vis=True` 单按 V 因 `not True=False` 反而关，需正确 `set_debug_vis`，未深究）。
- **LeSONIC/SONIC.ipynb ④ 区**：自训全流程一键（4.1 record / 4.2 convert / 4.3 finetune / 4.4 open_loop_eval / 4.5 dump / **4.6 七条 GR00T 驱动动作各一键 `gear_sonic_inject.sh <motion>`** / 4.7 老师vs学生并排）。③ 区是 SONIC 原生动作一键。
- **prompt 性质澄清**：训练已 prompt-conditioned，但当前是**开环回放**（离线 dump token 再注入），**非实时下 prompt**。实时"敲 prompt 当场动" = GR00T 在环 live get_action（in-process 或官方 ZMQ）+ 需泛化。
- **✅ 已整理待提交（2026-06-03 收尾，用户自己 commit）**：
  - **submodule 编辑全转 patch + apply 脚本**：① WBC 新文件 `vla_token_{recorder,injector}.py` 被 WBC `.gitignore:209 data/` 吞（`git diff` 抓不到，**必须 force-add**）→ `LeSONIC/patches/gear-sonic/0003-vla-token-recorder-injector.patch`（396 行 creation diff）；② Isaac-GR00T `launch_finetune.py`+`experiment.py` 6 行 → `LeSONIC/patches/gr00t-n17/0001-finetune-oom-fit-adafactor-gradckpt.patch` + 新 `LeSONIC/scripts/apply_gr00t_n17_patches.sh`（照搬 `apply_gear_sonic_patches.sh` 幂等模式）。两 apply 脚本 dry-run 全 SKIP。
  - **归属厘清**：WBC `features_sonic_vla.py`/`exporter.py` = WBC 已 tracked 提交；`unitree_g1_sonic` embodiment = Isaac-GR00T **已 commit**（3df8b38），非我改动不用 patch；`download_from_hf`/`g1.py` 由 gear-sonic 0001/0002 覆盖；Isaac-GR00T `lerobot_episode_loader`(mmap)+`server_client`(N1.5/6/7 wire)+`pyproject/uv.lock` = **早先 LeIsaac 工作流**不归本路线。
  - **自愈脚本固化**：`/tmp/{resume_8k_heal,keep_stage_ckpts,ckpt_intact}` → `scripts/gr00t_{resume_heal,keep_stage_ckpts,ckpt_intact}.{sh,py}`，硬编路径提成 `OUT`/`MAX_STEPS`/`STAGE` env，`/tmp` 原件已删。
  - **gitignore**：`outputs/`(33)+`datasets/`(43) 整目录已忽略 → 无需改；零大文件入 git。
  - **doc**：`LeSONIC/doc/groot_sonic_wbc_route.html` 加 §10 derisk 复盘（管线 SVG + MSE 表 0.065→0.0011 + 诚实边界 + 绿老师死路 + 下一步闭环 + 复现脚手架表）。
  - **本 repo 待 commit**：`scripts/{gear_sonic_record,gear_sonic_inject,gr00t_sonic_finetune,gr00t_dump_pred_tokens,sonic_vla_npz_to_lerobot,gr00t_resume_heal,gr00t_keep_stage_ckpts,gr00t_ckpt_intact,apply_gr00t_n17_patches}` + `patches/{gear-sonic/0003,gr00t-n17/0001}` + `LeSONIC/doc/groot_sonic_wbc_route.html`(M) + `SONIC.ipynb`(M) + `.memory`(M)。submodule pointer `Isaac-GR00T`(m)/`lerobot`(m) 不提交。
  - **磁盘提醒**：`outputs/gr00t_sonic_8k` 83G（rolling 7000/7500/8000 + keep/2000-8000）、`outputs/gr00t_sonic_derisk` 47G（2000 步旧 run，**已被 8k 完全取代**，可清）。共 130G。
- **下一步（真问题）**：① held-out 泛化（新 prompt/新起始/新 clip 测 MSE，现 7-ep 过拟合）；② 实时 prompt 活循环；③ 多 clip+RSI 扩数据（DR 放量实测 95% 重复无用）；④ 闭环纠偏治长动作开环漂。

## ✅ 闭环活循环已跑通 + 关键发现（2026-06-04）
- **闭环桥架构**：GR00T N1.7（transformers 4.57.3 venv）与 SONIC eval（Isaac conda）**不能同进程** → GR00T 自带 ZMQ server (`gr00t.eval.run_gr00t_server --model_path <ckpt> --embodiment_tag unitree_g1_sonic --port 5555`) + Isaac 端独立 wire client。脚手架：`LeSONIC/scripts/gear_sonic_live.sh <motion>` + `gear_sonic/data/vla_live_injector.py`(VlaLiveInjector callback，**data/ 被 gitignore 同 recorder/injector，转 patch 要 force-add**) + `LeSONIC/doc/sonic_vla_closeloop_validation.html`。
- **wire 协议**：`PolicyClient.get_action(obs)` 包成 `call_endpoint("get_action",{"observation":obs,"options":None})`，server 端 `handler(**data)` 展开 → 必须键名 `observation`；返回 `(action,info)`，token 在 `action["motion_token"]` (无 prefix，需 `[0]` 去 batch)。序列化 = `msgpack_numpy.packb/unpackb`。conda env 有 zmq/msgpack_numpy。
- **obs 组装（1:1 复刻 converter）**：video/state/language 全 `delta_indices=[0]`（**只当前帧，无历史窗，不缓冲**）。state = `_fit(robot.data.joint_pos,43)` 按 `meta/modality.json` 切 left_leg[0:6]/right_leg[6:12]/waist[12:15]/left_arm[15:22]/left_hand[22:29]/right_arm[29:36]/right_hand[36:43] + `compute_projected_gravity(root_quat)`(3) + ego cam `scene["ego_camera"].data.output["rgb"]`(480×640) + prompt。每组 reshape (1,1,d)。**server 内部归一化 → 发原始值**。action_horizon=40 → 每 40 步查一次缓冲逐步喂。
- **🔑 核心发现（无记忆 obs 条件策略的本质）**：GR00T 单帧 obs→token，闭环里**只能延续观测到的姿态相位**，无法自发从静止重启一次性动作。逐层诊断证 GR00T 没坏（live token 正确匹配对应相位 dump token）、obs 没喂错（state L2=0.05 匹配录制帧，cam mean 对得上）。
- **P2 全 7 动作扫描（dpose=查询间关节位移 L2，判动/不动）**：**自持 ✅** squat/lunge/dance/macarena（dpose ~1.5-2.5）；**卡静止 ❌** 仅 kick(3.3s单踢→0.06)、walk(走+转身停→0.06)；jump 边缘。判别 ≠ 严格周期性，而是「是否 settle 到策略逃不出的通用站立姿」。**先前"周期vs一次性"二分被全扫描推翻，如实修正。**
- **P-fix 触发式 bootstrap（免重训救一次性动作）**：`VlaLiveInjector` 加 `bootstrap_npz`+`bootstrap_steps`，复位后前 N 步喂开环 dump token 带进动作再交 live。`BOOTSTRAP=80 bash LeSONIC/scripts/gear_sonic_live.sh kick` → dpose 0.06→**1.42 持续踢**。证明 live GR00T 能接续中段动作。
- **踩坑**：① IsaacLab `app_launcher.py:561` 读 env var `HEADLESS` 做 `int()` → `HEADLESS=True` 泄漏崩 `int('True')`；脚本读完 `unset HEADLESS` 只用 `+headless=` arg。② headless 下 `debug_vis=True`(绿骨架)报错，只 GUI 加。③ 单 GPU 连跑多 eval，timeout 杀 wrapper 但 python 子进程 orphan 占 GPU，累积 4 个撑爆 24G(`create_articulation_view:NoneType`)→ run 间按 PID 显式 kill。④ `pkill -f eval_agent` 自杀（当前 shell 命令行含该字串）→ 按 PID 过滤排除 `$$`。

## 🚀 已发布到 HF（2026-06-04）
> **2026-06-09 改名（按动作源分变体）**：`SONIC-VLA-LeRobot`→`SONIC-VLA-BonesSeed`、`GR00T-N1.7-G1-SONIC`→`GR00T-N1.7-G1-SONIC-BonesSeed`（用 `HfApi().move_repo`，HF 自动留旧名 redirect，旧 URL 仍 200）。原因=正开发并行变体 `SONIC-VLA-LAFAN` / `GR00T-N1.7-G1-SONIC-LAFAN`（LAFAN 动作源），同一 `prompt→token→WBC` 管线、只换动作源。改名只动 `wsagi/`-前缀标识符与 `huggingface.co/...` URL，**不碰本地 `录屏/GR00T-N1.7-G1-SONIC-*.webm` 真实文件名**；collections script 也改新名（幂等，无重复）。
- **模型** [`wsagi/GR00T-N1.7-G1-SONIC-BonesSeed`](https://huggingface.co/wsagi/GR00T-N1.7-G1-SONIC-BonesSeed)：checkpoint-8000 推理权重（3 shard 4.7/4.7/2.5G）+ 7 个闭环 demo mp4 内嵌 model card（`<video>` + resolve full URL）+ 诚实 scope（仅训练集/kick&walk需bootstrap/依赖SONIC栈）。`base_model: nvidia/GR00T-N1.7-3B`（**实测起点，非 Cosmos**；Cosmos-Reason2-2B 只是内部冻结 VLM backbone）。22 文件。
- **数据集** [`wsagi/SONIC-VLA-BonesSeed`](https://huggingface.co/datasets/wsagi/SONIC-VLA-BonesSeed)：LeRobot v2.1（7 动作 3.3M）+ 7 ego mp4。**血缘**：`source_datasets: bones-studio/seed`（SONIC 训练源）+ 正文链 `nvidia/GEAR-SONIC`（直接上游：产 token 的 WBC + demo_robot_filtered.pkl 的出处）。23 文件。
- **血缘闭环**：`bones-studio/seed → nvidia/GEAR-SONIC → wsagi/SONIC-VLA-BonesSeed → wsagi/GR00T-N1.7-G1-SONIC-BonesSeed`。
- **录屏源**：`~/视频/录屏/GR00T-N1.7-G1-SONIC-*.webm`（7 条），转 mp4 用 **系统 `/usr/bin/ffmpeg`**（conda ffmpeg libx264.so.138 坏）+ `env -u LD_LIBRARY_PATH` + `-vf scale=trunc(iw/2)*2:trunc(ih/2)*2`（录屏 496x367 奇数高，H.264 要偶数）。
- **上传踩坑全记进 [[hf-upload-tricks]] + skill `hf-publish-model`**：多 GB 目录走代理别用 upload-large-folder（批量 commit 整条断），用逐文件 + **每次 `timeout` 包住**（99% 是 commit hang 不报错，retry 循环不触发）；`publish.sh` 已重写成 per-file+timeout。

## ⚠️ 闭环阶段新未提交（接着上次"你整理用户自己commit"那批之后）
- 本 repo：`LeSONIC/scripts/gear_sonic_live.sh`(新,含 HEADLESS/BOOTSTRAP/RELAX) + `LeSONIC/doc/sonic_vla_closeloop_validation.html`(新) + `SONIC.ipynb`(M?) + `.memory/{sonic-wbc-vla-route,hf-upload-tricks}.md`(M)。
- WBC submodule：`gear_sonic/data/vla_live_injector.py`(新,**被 data/ gitignore 吞,转 patch 要 force-add**,同 0003 套路 → 建 `LeSONIC/patches/gear-sonic/0004-vla-live-injector.patch`)。
- 全局 skill/memory（在 `~/.claude/`，不在 repo）：`hf-publish-model` SKILL.md + scripts/publish.sh 已改;`hf-upload-tricks.md` 已补。

## 🎬 时序 prompt 串接（2026-06-09，单会话多动作，免重启 GUI/server）
**用户要"一句话序列驱动 squat→walk→kick、录像平滑"。已实现（GPU-free 全验，待显卡录像）。**
- **底层支撑（实锤）**：GR00T server **无状态**，prompt 每 query 放在 obs 的 `language` 里 → 中途改 `self.prompt` 下次 query 即生效，**server/GUI 都不用重启**。这是时序换 prompt 能成立的根。
- **能力边界（诚实，已写进 launcher 注释）**：这是**对记忆的 7-skill 模型做 prompt 编排（orchestration）**，不是复合 prompt 理解；仍 RELAX。**neutral-hub 串接只治 (A) 起姿 off-distribution，不治 (B) 从中性姿重新触发歧义** → one-shot 段（kick/walk/jump）仍需 per-segment bootstrap 才离开站姿。根治要 P1 相位/记忆。自持四动作（squat/lunge/dance/macarena）串接最干净。
- **脚手架**：① `vla_live_injector.py` 升级为 **timeline 驱动**（向后兼容单 prompt = 单无限段）：构造器加 `timeline_json`；`_build_segments/_current_segment/_pick_source` 段内顺序 = settle(neutral 或 freeze last)→bootstrap→live；段切换在 `eval_step` 换 prompt+强制重 query。② `LeSONIC/scripts/gr00t_build_sequence.py`（纯 stdlib，`--seq squat,walk,kick` 或 `squat:150,kick:180`；ONESHOT={kick,walk,jump} 自动挂 bootstrap_npz=PRED_DIR/<key>.npz；emit `total_steps`）。③ `LeSONIC/scripts/gear_sonic_sequence.sh`（建 timeline→launch）。
- **🔑 两个躲过的运行时坑**（grep 源码确认，非猜）：① **`time_out`**（`tracking_time_out`，单 motion 播完即重置）必须 `++manager_env.terminations.time_out=null`；② **`episode_length_s`**（`base_env.yaml:31`=10.0，IsaacLab **时间截断**，绕过 time_out！）必须按总步数拉长 = `++manager_env.config.episode_length_s=total_steps*0.02+5`（控制 dt=decimation4×sim_dt0.005=0.02s=50Hz；默认 10s 只够 500 步，600 步序列会砍在 kick 中段）。
- **patch**：`vla_live_injector.py` 在 gitignore 的 `data/` → `0004-vla-live-injector.patch` 用 `git diff --no-index` 重生成。`manager_env_wrapper.py`(tracked) 的 **C 键近距视角** → `0005-viewer-snap-to-robot-key.patch`(普通 modification diff，`git apply --check --reverse` 验过)。
- **一键编排 + notebook（2026-06-09 跑通验证）**：`scripts/gear_sonic_live_demo.sh` = **自动起 GR00T server(幂等,pgrep 查 port 5555,缺则 Isaac-GR00T/.venv 起+等"Loading checkpoint shards: 100%")→ 再开 viewer**(server/viewer 两 env 不同所以要编排);`scripts/gear_sonic_stop.sh` = 杀 viewer+server 释放显存(**bracket `[e]val` 防 pgrep 自匹配**)。`SONIC.ipynb` 加 **⑤ 区**(5.0 list / 5.1 @flow1 / 5.2 @flow2 / 5.3 即兴+视角键 / 5.4 stop)。
- **视角/可视化坑(GUI demo 必调)**：① 默认 viewer 上帝视角太远 → launcher `++manager_env.config.viewer.eye/lookat` 改近距全身(`eye=[2.2,2.2,0.5] lookat=[0,0,-0.1]`,lookat z 偏低才看得到腿;CAM_EYE/CAM_LOOKAT env 可调);wrapper 加 **C 键 `snap_to_robot`**(非 toggle,SONIC_CAM_OFFSET 调)。② **黄/红球(goal/body marker)跟的是加载的 squat 参考(原地+8.5s循环复位),机器人被注入 token 驱动走开→对不上**,无轻量修法 → launcher 默认 `++manager_env.commands.motion.debug_vis=False` 隐藏。③ **`pgrep -f eval_agent_trl` 会匹配自己的诊断命令** 造成"进程反复重启"假象 → 用 `ps|grep '[e]val_agent_trl'` bracket 技巧。
- **🕰️ 三时钟坑(动作串联调试核心,2026-06-09 三方验证)**：GUI"闪回原点"+"卡死"根因 = **三个独立时钟只关了俩**。①Gym episode 钟(time_out=null+拉长 episode_length_s,关了)②**TrackingCommand motion 钟**(`_update_command` 每步+1,clip 末 424帧≈8.5s 触发 `_resample_command`→`write_root_state_to_sim` **1-frame 硬传送机器人回参考原点,绕过 dones**=真凶,我漏了)③注入器 global_t(modulo,我误以为代表一切)。**dones 探针看不到②**(soft command reset 非 termination);g1_dyn decoder 只吃 token+proprio **不含 motion_anchor_pos_b**→"WBC 锚点拽回"框架是错的(codex+mimo 都纠正)。修=`SONIC_NO_REF_RESAMPLE=1`(patch 0006)**冻结**参考时钟(不推进不重采样,边界不触发)。**二次坑**:第一版用"循环时钟"(time_steps=-motion_start)RSI 非零→time_steps 负→帧越界 NaN→物理炸 GUI 卡死;**必须 freeze 不能 loop**。实测:walk 推到-1.9m,修复后 macarena 段留-2.4m(走到哪作哪),修复前弹回-0.07。terrain=plane 把 env-0 放回世界原点(治相机闪);debug_vis=False 隐藏黄红球(marker 跟 squat 参考对不上注入动作)。
- **doc**：`LeSONIC/doc/sonic_action_sequencing.html`(动作串联全记录:架构+段内三窗口+flow1/2+一键+**§4 三时钟调试实录+freeze 修复**+GUI 坑表+诚实边界;单文件内嵌 SVG)。
- **待 commit（用户自己）**：`scripts/{gear_sonic_sequence.sh,gr00t_build_sequence.py,sonic_demo_flows.json,gear_sonic_live_demo.sh,gear_sonic_stop.sh}` + `patches/gear-sonic/{0004,0005,0006}` + `SONIC.ipynb`(⑤区) + `doc/sonic_action_sequencing.html` + `.memory`。
- **待 GPU/录像**：`bash scripts/gear_sonic_live_demo.sh @flow2` 一键跑;平滑录像走 render-to-mp4(`render_results=true`+`save_rendering_dir`,避开 live viewer 的推理 stall);实时提速要 server bf16(动 Gr00tPolicy 加载)。held-out 闸门 + RELAX=0 严格重测仍是 go/no-go 主线。

## 🥋 LAFAN 扩容能不能融进 SONIC-VLA（2026-06-09 三模型头脑风暴 + codex 复审文档）
**结论：「融合策略」是伪命题。** MimicKit-G1-LAFAN = 29-DoF DeepMimic PPO（吐关节角），SONIC-VLA = GR00T→64维FSQ token→冻结WBC解码；**动作空间+架构都不同，权重不可合并**。LAFAN 唯一入口 = SONIC token 空间，被硬闸门卡：**冻结 WBC 能不能在物理上跟住**。GR00T 技术上能吐任意连续 token（decoder 照收），但只有落在 decoder 训练过的 FSQ 流形附近才解出稳定动作（=部署侧 snap-to-grid 的原因）→ WBC 跟不住的动作，训 GR00T 也只得不稳行为。
- **四条路排名**：① Route A=LAFAN参考→robot_filtered→**严格**WBC跟踪筛查→过则录token→finetune GR00T（先做筛查那步,**这步是「WBC能力筛查」不是「扩VLA」**,codex 纠偏）；② Route C=双控制器路由(SONIC-WBC‖MimicKit策略,运行时按prompt切,复用动作串联框架)=**唯一现在能立刻多动作做demo**的,但两套系统共用机器人**不是扩VLA**；③ Route B=MimicKit策略当数据生成器(rollout→可行轨迹→SONIC跟踪);④ Route D=重训/微调WBC本身(根因,最贵,最后)。
- **扩容阶梯**:13 deploy demo✅→**BONES-SEED G1子集**(converter已原生支持flat CSV `load_bones_csv`,WBC同源分布,风险最低零新代码,但每条仍要逐条过筛查)→**LAFAN最后**(需写MimicKit→robot_filtered新converter+物理gap+MimicKit自家run仅63%跟踪=参考部分不可行)→全量SMPL。
- **两道必过闸门(碰LAFAN前)**:①严格WBC跟踪筛查**RELAX必须关**(codex盲点:demo脚本放宽阈值/关time_out是筛查毒药;用release自带`base_adaptive_strict_ori_foot_xyz`不用`tracking/eval.yaml`;判据`im_eval_callback`报terminated/success_rate/逐动作progress/mpjpe_g,**先定阈值再跑**)；②VLA held-out泛化LOMO(`make_holdout_split.py`,7动作×1ep纯记忆,不过则加LAFAN=放大查找表)。
- **第一个实验**(一条clip转好后~30min):严格headless im_eval,先kick(in-dist标基线+验harness)→再LAFAN fight(真风险)。无GR00T/recorder/MimicKit/RELAX/相机。A过B过→Route A继续;A过B垮→停止用LAFAN token,转C(demo)或D(根因)。
- **核实的事实纠正**:FSQ `num_fsq_levels:32`(不是16),部署token值snap到1/16网格17值[-0.5,0.5](`0004` patch);离线`encode()`非免费(要`command_multi_future_nonflat`+`motion_anchor_ori_b_mf_nonflat`精确FK特征,且token仍要WBC解得出才有意义)。
- **🥋 实测结果(2026-06-09真跑了严格WBC筛查,推翻一刀切悲观)**:写了 `scripts/lafan_mimickit_to_robot_filtered.py`(MimicKit 35维frame=[root_pos3,root_expmap3,dof29]→robot_filtered;**joint_order=mj**经18mm实测验证对;converter只读pelvis idx0其余body填零;**fps留30由fk_batch加载时重采样到50别relabel否则变速**);im_eval需`smpl_sim`本机没装→`/tmp/smpl_stub`轻量stub还原compute_metrics_lite(terminated/progress由im_eval自算不受影响)。**严格筛查(release自带`base_adaptive_strict_ori_foot_xyz`+无RELAX/相机/GR00T,`++eval_callbacks=[im_eval] ++eval_output_dir=...`,num_envs=N并行,os._exit(0)干净退)**:
  - **in-dist 7条基线**:6/7过(success0.857),**macarena(27.5s最长)严格下也摔@37%**→strict闸门是真硬,长动作累积漂移触发,这才是demo当初要RELAX的原因;过的MPJPE_l 22-42mm。
  - **LAFAN 4条**:**fight_15s(含高踢腿主段)严格全过✅ progress1.0 全局漂移MPJPE_g 372mm=in-dist同档=WBC真能跟**;jumps_15s摔@2.5%、dance_15s摔@6%、run_15s摔@61%。RELAX(foot/anchor/ori阈值=99)后4条都跑完不终止,**但MPJPE_g照妖镜**:fight372/dance506/run853/**jumps2345mm(2.3米!身体根本不在参考处=没做成跳)**。
  - **🎯物理规律**:SONIC WBC是**接地平衡/接触控制器**→接地动态动作(fight高踢腿/lunge)跟得住,弹道腾空(jumps双脚离地走抛物线)跟不住,带飞行相locomotion(run)部分跟漂移大,外部dance编舞边缘。
  - **对用户问题"能不能用LAFAN加更多动作"的答案**:**fight能**,jumps不能(WBC物理做不出),run/外部dance边缘。**动作类型级go/no-go不是LAFAN整体不行。** 原始json `/tmp/sonic_screen/{indist,lafan_all,lafan_relax}/metrics_eval.json`(runtime)。
- **🏆 fight走完整管线=真加进VLA了(2026-06-09当天跑通,~1h)**:**录token(760帧,prompt"throw punches",`gear_sonic_record.sh`一off,max_steps=760覆盖30→50重采样后749帧)→建8动作LeRobot数据集(`sonic_vla_lerobot_8`,9ep因raw里kick有2个npz,fight=traj2)→finetune GR00T 6000步(`gr00t_sonic_finetune.sh` DATASET/OUT_DIR/MAX_STEPS env,loss1.19→0.044,1.85it/s单4090~55min,22.6G/85%util)→open-loop MSE**。**结果:fight MSE 0.0021落在原7动作区间(0.0007-0.0034)正中,比kick/squat还低,比per-motion-mean基线0.039低18.7×→GR00T学会吐fight token和原有动作一个量级=fight已进VLA**。ckpt `outputs/gr00t_sonic_8plus_fight/checkpoint-6000`(gitignored)。
  - **诚实边界**:开环+训练集轨迹MSE=记忆,和已发布7动作同性质(都没过held-out)。证明"架构能吸收新动作",非泛化。闸门②LOMO held-out对所有动作(含fight)仍悬而未决=下一个真问题。
  - **复跑踩坑**:① converter `sonic_vla_npz_to_lerobot.py`在`.venv_data_collection`跑要`PYTHONPATH=<WBC dir>`否则`No module named gear_sonic`(脚本dir上path非cwd);② 读npz/dataset info要等writer进程真退(`until ! kill -0 PID`)否则BadZipFile/读到半截;③ converter增量写info.json,EP数中途不准要等DONE。
  - **闭环GUI跑通**:server起在Isaac-GR00T/.venv指checkpoint-6000;`gear_sonic_live.sh`加了fight(key=lafan_fight_15s,prompt"throw punches");bootstrap复用录的npz(有motion_token键)放`datasets/sonic_vla_pred_fight/lafan_fight_15s.npz`,BOOTSTRAP=80;实测dpose 1.0-1.9持续、root z 0.6-0.75不摔=GR00T自主出拳。
## ⭐ POST-COMPACT 待执行:用户手挑窗口训 flow3(2026-06-10)
**用户在`LAFAN_segments.ipynb`填好窗口(运动学回放选的),要训 fight×5 + run×3 + dance×2(jumps无)。** 窗口(秒,从full clip切):
- **fight**(`data/fight_full_robot.pkl` key=fight_full):10-25=`block, push-kick, and shove` / 70-80=`palm strike, kick, and hook punch` / 99-119=`continuous throwing swings` / 124-139=`continuous fierce swings` / 214-229=`combat strikes and combo kicks`
- **run**(`run_full`):13-28=`jog forward then run backward` / 42-57=`sprint back and forth then backpedal` / 104-119=`run in a circle`
- **dance**(`dance_full`):37-57=`moonwalk` / 66.5-86.5=`spin, step back, and clap`
**管线**(每窗口):① `cut_motion_window.py --input data/<clip>_full_robot.pkl --key <clip>_full --start_s A --end_s B --output data/seg_<name>.pkl --out_key <name>`→ ② **严格WBC筛查**(无RELAX,terminated=False+progress~1=可track)→ ③ 过的录token(eval+`vla_token_recorder`带prompt)→ ④ `sonic_vla_npz_to_lerobot.py`(PYTHONPATH=WBC dir)建数据集 → ⑤ `gr00t_sonic_finetune.sh`训 → ⑥ flow3=离线拼token时间线`gear_sonic_inject.sh`(无server不卡)。
**⚠️ 风险**:① ~~prompt未定~~ **prompt已定(见上,用户注释翻译的英文)**,直接用作训练task串;② **dance大概率筛查就摔**(冻结WBC跟不住dance,dance_15s严格@6%垮;这俩dance窗口各20s长)→录不出有效token→告诉用户dance进VLA得重训WBC(deferred),flow3先fight+run。fight/run窗口也偏长(15-20s),长动作累积漂移可能筛查失败(macarena 27.5s栽过),逐个筛。脚手架:`cut_motion_window.py`/`gear_sonic_replay_window.sh`(运动学不摔)/`gear_sonic_preview_window.sh`(WBC筛查,摔=不可track)/`gear_sonic_record.sh`/`sonic_vla_npz_to_lerobot.py`/`gr00t_sonic_finetune.sh`/`gr00t_dump_pred_tokens.py`(有--keys)/`gear_sonic_inject.sh`(有skill,离线无freeze)。

### 🚨 严格筛查实测(2026-06-10):10窗口仅2过,推翻"fight/run应过"预测
新脚手架 `scripts/gear_sonic_screen.sh`(可复用,STRICT无RELAX,num_envs=keys数一次跑,读`eval/all_metrics_dict`逐key terminated/progress/mpjpe_g,PASS=not term且progress≥0.95;坑=多key `filter_motion_keys=[k1,k2]`要方括号不是裸逗号否则Hydra Ambiguous)。10段合一`data/seg_flow3_all.pkl`,strict结果 **2/10 PASS**:
- ✅ **run_jog_backward**(13-28,prog1.0,mpjpe_g216) ✅ **run_sprint_backpedal**(42-57,prog1.0,mpjpe_g477)
- ❌ fight全5个垮(block_pushkick_shove@0.598/palm_kick_hook@0.096/throwing_swings@0.703/fierce_swings@0.095/combat_combo_kicks@0.310)、run_circle@0.904(差一点)、dance_moonwalk@0.465、dance_spin_stepback_clap@0.166。
- **根因**=用户手挑窗口都是**10-20s长复合动作**,长动作累积漂移触发strict adaptive终止(`整段失败≠段级失败`已知规律:之前fight 2-4s短段过11/14、run短段过14/14,但15s整段run严格@61%垮)。fight整15s"高踢腿主段"之前过是因为那段特定可跟,这5个窗口内容不同。
- **决策悬置(用户的)**:用户**主动放弃过自动切割**(嫌切坏),不能擅自重切短段(=revert他的决定)。已用 AskUserQuestion 给4选项。用户选=**先让 codex gpt-5.5 + mimo 批判训练方法找救活机会,再决定**;并追问"为啥没训练多久就判摔(其实筛查阶段没训练,是冻结WBC跟踪,早摔=严格阈值早触发)"+"WBC有改进方法吗调研"+"SONIC数据工厂能不能变形动作让WBC不摔"。原始verdict json `/tmp/sonic_screen/flow3/metrics_eval.json`。

### 🎯🎯 三方批判 + 物理可行性筛查=救活6/10(2026-06-10,推翻"2/10")
**codex+mimo+我数据强三方收敛:严格闸门把「碰安全边界」当「物理不可行」,这俩不是一回事。** mpjpe_l处处在分布内(33-46mm,in-dist也22-42)却8/10被杀=瞬时单帧触发`ee_body_pos`(手脚高>15cm,单帧)/`foot_pos_xyz`(脚踝>20cm,单帧),非真摔。mimo给精确阈值:anchor_pos根高>15cm、anchor_ori_full躯干>25.6°(0.2rad²)、ee/foot单帧越界。**两模型都把"instrument哪个term先触发+用actual-fall(根高+倾角)重分类"列第一优先**。
- **新脚手架**:`gear_sonic/data/phys_valid_screen.py`(eval-callback,**关掉4个strict偏差终止项**只看WBC物理跟踪时机器人**真摔没**=根高<0.6m或躯干倾>40°;读`robot.data.root_pos_w[:,2]`+`projected_gravity_b`;env→key用`env.motion_command.motion_ids`+`motion_lib._motion_data_keys`;坑=`env`是ManagerEnvWrapper无`.scene`,用`_resolve_scene`抄recorder的`e.env.scene`)+ `scripts/gear_sonic_phys_screen.sh`(terms=null+phys_valid callback)。**转patch时data/被gitignore要force-add同recorder**。
- **🥋 物理筛查实测(10窗口,真摔判据)**:**6/10从头到尾UPRIGHT没摔**:run×3全过(jog z0.71/sprint z0.62/circle z0.66,circle严格只差@0.904)+ **dance×2全过(moonwalk z0.64 tilt17.7/spin-clap z0.67 tilt23.1——严格被旋转倾角>25.6°误杀,实际从没真摔!)** + **fight_combat_combo_kicks z0.65 tilt39.1 UPRIGHT**。2个长前缀:block_pushkick_shove摔@step542/749=**前72%有效**、fierce_swings摔@241/749=**前32%**。2个坏起始即摔:palm_kick@step15(3%)、throwing_swings@51(5%)=窗口起始是过渡帧→RSI/start-offset后续救。
- **📌 关键诚实caveat**:UPRIGHT=「没摔」≠「忠实跟出该动作」。关掉终止后WBC可能漂着站住(如moonwalk严格run的mpjpe_g 483mm偏大=可能只是站着晃不是真漫步;dance_clap mpjpe_g90mm优、combat187优)。**录制判据=物理有效(没摔→token稳定可realize),保真度=GUI视觉筛(既定:MSE好≠闭环视觉好)**。token编码的是WBC真做出来的,部署侧VLA吐token→WBC解→机器人也就做这个,所以稳定即token有效;像不像该动作留GUI筛。
- **WBC改进调研(Explore agent验源码)**:① warm-start finetune **支持**(`train_agent_trl.py:167 resume_checkpoint`+README示范`+checkpoint=sonic_release/last.pt`),但**FSQ码本虽是固定网格(num_fsq_levels=32无学习参数,token值survive),decoder MLP会被重训→同一token解出的动作变了→已发布BonesSeed要重录**=不free;4096env单GPU 4-24h。② **residual adapter**(冻base decoder加小修正网络)=最干净保token空间,但非released栈原生。③ curriculum软→硬终止配合finetune。④ reference-conditioning对弹道相位foot/ee降权。per-family WBC=死路(碎片化decoder)。
- **🏭 SONIC数据工厂"变形"调研(用户想法,Explore验)**:**`motion_fps_scale`是0成本配置旋钮**(`motion_lib_base.py:221`,`++...motion_lib_cfg.motion_fps_scale=0.6`全局慢放→削快拳速度尖峰让foot/ee不越界);load时fk_batch已lerp+slerp重采样30→50fps;无现成butter/savgol→可写~50行numpy后处理(savgol平滑快关节dof/根漂线性去趋势/脚高clamp);`zero_root_xy`只清初始XY不治累积漂。**最低成本变形=motion_fps_scale慢放,其次savgol削速度尖峰**(都不碰WBC)。
- **✅ flow3全管线跑通(2026-06-10)**:录8个物理有效窗口→`datasets/sonic_vla_lerobot_flow3`(8ep/5777帧)→finetune checkpoint-6000(train_loss0.052,step2064偶发CUDA`invalid resource handle`崩=已知gotcha非配置问题,从ckpt-2000 resume完成)→**open-loop MSE平均0.00257全8traj 0.0016-0.0031全在已知好区间≤0.004=GR00T学会全部8窗口token**→dump预测token`datasets/sonic_vla_pred_flow3/<key>.npz`(8个)→headless inject smoke验证(injector载token+hook装好loop=True+无早reset+无错)。LAFAN.ipynb重建为8窗口分组(fight/run/dance)每cell`PKL=data/seg_flow3_all.pkl PRED_DIR=$PWD/datasets/sonic_vla_pred_flow3 NULLTERM=1 bash scripts/gear_sonic_inject.sh <skill>`。
- **新脚手架/改动(待commit)**:`scripts/{gear_sonic_screen.sh,gear_sonic_phys_screen.sh,gear_sonic_record_flow3.sh}`新;`gear_sonic_inject.sh`(加8 flow3 key+**NULLTERM模式**:adaptive终止项忽略threshold=99必须null);`LAFAN.ipynb`重建;WBC submodule `gear_sonic/data/phys_valid_screen.py`新(data/被gitignore转patch要force-add);`.memory`。
- **🔑 inject关键坑**:flow3窗口触发**adaptive终止**(anchor_pos/ee_body_pos),`RELAX=1`的`threshold=99`对adaptive**无效**(它重算自己阈值)→开环inject会1-2s reset→**必须`NULLTERM=1`把4个term设null**。
- **🔁 flow3循环demo已搭(2026-06-10,用户定序fight1-run3-fight2-run2-dance1-run1)**:映射=block(子段1前缀)→circle→combat(子段5,替被丢的子段2 palm_kick)→sprint→moonwalk→jogback。脚手架`scripts/build_flow3_sequence.py`(拼6窗口预测token→一条(4537,64)时间线npz,90.7s/圈,--blend可seam交叉淡化)+`scripts/gear_sonic_flow3.sh`(SEQ短名列表→建timeline→离线inject loop)。**三时钟修复必带**(否则在首窗口参考长度542步处reset打断):`time_out=null`+`episode_length_s=36000`+`SONIC_NO_REF_RESAMPLE=1`+4偏差term null。headless smoke验证220s跑满无mid-seq reset(2次Sampling motion都是启动双载)。token时间线1步1token@50Hz对齐,轨迹时序正确。
- **🛰️ flow3 live实时推理版也加了(2026-06-10,用户要)**:复用live序列脚手架(`gear_sonic_live_demo.sh @flow3`→起GR00T server+viewer+`gear_sonic_sequence.sh`+`vla_live_injector`)。改3处:`gr00t_build_sequence.py`(加8 flow3窗口KEY/PROMPT精确匹配训练task+全列ONESHOT因都mid-action起)、`gear_sonic_sequence.sh`(加8 KEY)、`sonic_demo_flows.json`(加`flow3`=block/circle/combat/sprint/moonwalk/jogback 6段,每段短bootstrap80进动作后交live)。**server用flow3 ckpt**:cell=`GR00T_CKPT=$PWD/outputs/gr00t_sonic_flow3/checkpoint-6000 PKL=data/seg_flow3_all.pkl PRED_DIR=$PWD/datasets/sonic_vla_pred_flow3 bash scripts/gear_sonic_live_demo.sh @flow3`。验证=server用flow3 ckpt加载成功(3 shards 100% READY);build_sequence dry-run解析6段~91s OK。**诚实边界**:live窗口都one-shot,短bootstrap后交live GR00T可能渐站住(无记忆),离线loop才保证流畅。**LAFAN.ipynb两版flow3都有**(🔁离线流畅+🛰️live实时)+8单窗口cell+停止。**live会卡GUI**(ZMQ每40步阻塞主线程,已知),离线不卡。
- **🤗 数据集已发布(2026-06-10)**:[`wsagi/SONIC-VLA-LAFAN`](https://huggingface.co/datasets/wsagi/SONIC-VLA-LAFAN)(LeRobot v2.1,8窗口/5777帧,25文件,5MB)。卡片复刻BonesSeed+突出**物理可行性筛查**(LAFAN特有)+血缘(LAFAN1 Ubisoft→MimicKit retarget→phys-valid screen→SONIC WBC track→token;token producer仍GEAR-SONIC/bones-seed)+8 prompt。已加进**LeSONIC WBC** collection(`scripts/hf_make_collections.py`GROUPS加`SONIC-VLA-LAFAN`再跑,slug lesonic-wbc-6a258253bddb7b055363e4d5)。卡片在`datasets/sonic_vla_lerobot_flow3/README.md`(gitignored但已上传)。~~待发布~~ **模型也已发布(2026-06-10晚)**:[`wsagi/GR00T-N1.7-G1-SONIC-LAFAN`](https://huggingface.co/wsagi/GR00T-N1.7-G1-SONIC-LAFAN)=ckpt-6000转bf16(12.6→6.3G,`gr00t_resave_bf16.py`)+卡片(POC警告/物理闸门/血缘datasets:SONIC-VLA-LAFAN/base GR00T-N1.7-3B)+**9视频**(主flow3用户重录270s/65M+8子窗口,webm→mp4系统ffmpeg偶高修正);数据集README也加4预览(`media/{fight2,fight5,run3,dance2}.mp4`)。模型+数据集都进LeSONIC WBC collection。**上传双坑(新)**:①hf_transfer传完字节finalize卡死连接CLOSE-WAIT(代理掐)→**关hf_transfer用标准上传器**;②**HF LFS commit实测~15min,timeout设900s=自伤**(每轮在commit将成时杀掉无限循环98%)→**大shard timeout≥1800s**+resume cache让重试字节秒传;③起步偶发SSL UNEXPECTED_EOF秒败=瞬时抖动,套重试循环。
- **📄 原理文档(2026-06-10,用户要)**:`LeSONIC/doc/sonic_vla_principles.html`(自包含+内嵌3 SVG,讲:①架构token=什么/WBC=怎么 ②**PKL之谜**=不进驱动回路只做出生姿态+必填命令项占位+绿骨架,真机部署根本无参考 ③数据集6步管线 ④**两道闸门关键洞察**strict碰安全边界≠物理不可行,phys-valid真摔判据2/10→6/10 ⑤token录制物理意义FSQ固定网格 ⑥flow3离线vs live ⑦复现脚手架表)。样式遵feedback-html-doc-rules(color-scheme light+显式白底)。
- **⏱️ inject慢~2x原因(用户报)**:非bug非token拉长(injector每decode步推进1token,749token=749步=15s对齐)。根因=**live viewer实时率差**:inject每步全物理(articulation decimation4子步+接触)+WBC actor/decoder前向,replay只write_joint_state_to_sim(无策略无物理);Isaac GUI不锁实时→inject每步算得慢→画面慢动作→动态动作(踢/跑/转)力量感被抹平"看着不像"。**修=render-to-mp4按50fps**(`render_results=true`+`save_rendering_dir`,memory早记;待加RENDER_MP4模式)。
- **下一步(需用户GUI)**:用户跑`LAFAN.ipynb`逐个看8窗口+flow3循环→保真度筛(没摔≠像那动作,moonwalk mpjpe_g483疑虑/spin-clap90+combat187预期好)→留像的、弃只站着晃的、再定最终flow3序。坏起始2个fight(palm_kick@3%/throwing@5%)用RSI/start-offset或数据工厂motion_fps_scale慢放后续救。phys json `/tmp/sonic_screen/flow3_phys/phys_valid.json`,MSE/dump日志在`/tmp/sonic_screen/`。

- **🥋🏃 技能化拆分(fight/run切连贯招式做flow3,2026-06-09晚,二次三模型评审)**:用户决定=一个个技能训→GUI逐个演示留好的编flow3、差的不编、泛化推到flow3后。脚手架`scripts/lafan_segment_skills.py`(MimicKit clip→速度谷切连贯段[活动度=关节角速+根线速,深谷p25+≥1.2s间隔]→多key robot_filtered+manifest.csv含dur/horiz_travel)。fight1(245s)→80段,run1(238s)→63段。
  - **二次评审(codex+mimo)盲点**:①8细技能→砍3-4粗类(punch/kick/defensive/footwork),ego只看地面+细拳轨迹近同→**mode collapse几乎必然**;②flow3串接=编排非学组合,真瓶颈是**转场**→需指定guard为**中性枢纽**所有技能回它;③单clip"5样本"=零泛化多样性(相关重复);④held-out仍是真闸门(本轮推迟);⑤工具债:`gr00t_dump_pred_tokens.py`硬编range(7)+旧key、converter一段一prompt无转场上下文,扩段前要改。
  - **🎯段级严格筛查实测(28段:14fight+14run,推翻评审"run不值得拆")**:**fight 11/14过**(mpjpe_l13-41mm;seg030/040/062栽);**run 14/14全过**——**整段15s run严格垮@61%但短段(2-4s)全过**(短时长累积漂移不够触发终止)。run低位移转身段(0.01-0.27m travel,mpjpe_g43-60mm)最干净;高位移冲刺(8m)也不摔但全局漂~800mm(跟住步态落后地面覆盖)。**整段失败≠段级失败**。flow3 locomotion可用run转身/慢跑段(比单walk丰富)。段筛json`/tmp/sonic_screen/seg/metrics_eval.json`。
  - **doc**:`sonic_lafan_expansion.html`§8(技能化拆分+二次评审盲点表+段筛实测表)。
  - **✅ 14-skill模型训完(2026-06-09晚)**:录6候选技能(guard/quick jab/punch combo advancing/turn around/jog forward/run forward fast)→建`datasets/sonic_vla_lerobot_skills`(15ep)→finetune 6000步`outputs/gr00t_sonic_skills/checkpoint-6000`→open-loop MSE全15traj**全过0.0015-0.0033(均0.00218,≤0.004已知好区间)**,6新技能全学会。**坑**:`open_loop_eval.py:plot_trajectory_results`无条件画64行legend,matplotlib `_find_best_position`偶发崩(`input operand has more dimensions`)只出2traj→wrapper monkeypatch成no-op+`matplotlib.use("Agg")`重跑出全15(`/tmp/sonic_screen/eval_noplot.py`)。
  - **✅ GUI筛选脚手架就绪(2026-06-09晚)**:`LeSONIC/LAFAN.ipynb`(23 cells,一技能一cell点开闭环演示)+ `scripts/gear_sonic_serve.sh`(幂等起GR00T server,默认checkpoint-6000,GR00T_CKPT可override)。`gear_sonic_live.sh`已补6新skill的key/prompt(**prompt必须精确匹配训练task串**:guard/jab/combo/turn/jog/runfast→fight_seg000/020/050+run_seg001/006/017)。建了`data/skill_demo_robot.pkl`(7段合一)+`datasets/sonic_vla_pred_skills/<seg>.npz`(bootstrap symlink→录的episode)。
  - **🔴 坑+修(2026-06-09晚,用户点2个GUI都"无响应机器人没动")**:`gear_sonic_live.sh`**缺全部三时钟修复**(SONIC_NO_REF_RESAMPLE/time_out=null/episode_length_s拉长),只在长15s fight上测过没暴露;**短技能段(2-4s)每~2s撞clip边界→time_out重置+参考钟teleport→机器人秒重置looks卡死**。**修=改走flow/sequence路径**(`gear_sonic_sequence.sh`+`gr00t_build_sequence.py`本来就全有三时钟修复+循环+re-bootstrap+近距相机+debug_vis关)。**两处map都要加新skill**:`gr00t_build_sequence.py`的KEY/PROMPT/ONESHOT(加guard/jab/combo/turn/jog/runfast/fight,除guard外全列ONESHOT) + `gear_sonic_sequence.sh`的`declare -A KEY`(init-pose用)。LAFAN.ipynb各cell改成`PKL=data/skill_demo_robot.pkl PRED_DIR=$PWD/datasets/sonic_vla_pred_skills bash scripts/gear_sonic_sequence.sh <skill>`(LOOPS默认30循环~2min)。headless smoke验jab:dpose 0.38-0.44持续、z0.76不摔、无env RESET、无freeze=连续打拳 ✅。
  - **🔴🔴 真根因(用户提示BonesSeed.ipynb正常):live闭环路径(`gear_sonic_live.sh`/`gear_sonic_sequence.sh`,GR00T server在环)每40步同步ZMQ推理→阻塞Isaac GUI主线程→"过一会无响应"。BonesSeed的inject cells(29-35)用`gear_sonic_inject.sh`=离线回放预dump的token,无server无在环推理→不卡。** 修=**LAFAN.ipynb改走离线inject路径**(和BonesSeed一样,无server cell):① 推广`gr00t_dump_pred_tokens.py`加`--keys`(原硬编range(7)+旧key,codex早标);② 从checkpoint-6000 dump 6技能预测token→`datasets/sonic_vla_pred_skills/<seg>.npz`(traj 1/2/3/9/10/11);③ `gear_sonic_inject.sh`加6技能KEY;④ LAFAN.ipynb各cell=`PKL=data/skill_demo_robot.pkl PRED_DIR=$PWD/datasets/sonic_vla_pred_skills bash scripts/gear_sonic_inject.sh <skill>`(18cells,无server)。inject保留motion_timeout=循环播放,RELAX放宽终止。headless验inject jab:加载token+hook装好+无server+无错。**教训:给用户GUI看动作用离线inject不用live闭环(live只适合录像/真实时,且会卡GUI)。**
  - **🔪 切割/标注问题(2026-06-10,用户看演示发现:jab左手未刺出/turn像站桩/jog是跑后转弯)**:**根因=自动速度谷切割边界乱(切动作中间/跨转身/含站立空段)+ 按位移猜的prompt名错,非训练问题**(MSE≤0.004=模型忠实学了乱段)。几何实证:`run_seg001`「turn」yaw仅−1°(=站立空段,run clip开头warmup);`run_seg006`「jog」yaw+152°(其实是转身);`fight_seg020`「jab」yaw−142°(含大转身+切在combo中间);`fight_seg050`yaw−229°(多动作)。**两评审早警告"自动命名是瞎猜要可视化确认",我跳过了=错**。**预览工具**:① `scripts/preview_segments_fingerprint.py`(秒级无GPU,每段卡=俯视根轨迹+时长/位移/yaw转角/手臂std,直线=前进跑/原地+大yaw=转身/小点+手臂=原地拳/拐弯=多动作);② `scripts/gear_sonic_preview_motion.sh`(Isaac纯WBC跟踪任意PKL+KEY,无VLA无server,GUI看真实姿态,RELAX播完整段;**坑=必须`use_paired_motions=True`否则卡在motion-load不出窗口=用户报"没启动",已修;所有eval启动都要带这个**);③ `LeSONIC/LAFAN_segments.ipynb`(指纹总览inline+逐段Isaac实看+改KEY)。
  - **🔪 改手挑窗口(2026-06-10,用户嫌自动切割切坏)**:放弃自动段,改**按秒手挑**。4个完整clip全转好:`data/{fight,run,dance,jumps}_full_robot.pkl`(用bridge转lafan_{fight1,run1,dance1,jumps1},fight245s/run238s/dance131s/jumps244s)。新脚手架:`scripts/cut_motion_window.py`(切robot_filtered某key的[start_s,end_s]→1-key pkl,帧=秒×fps,fps从pkl读)+`scripts/gear_sonic_preview_window.sh`(`CLIP=fight START=22 END=25 bash ...`→切窗口→preview_motion.sh看)+`scripts/preview_clip_timeline.py`(整clip时间线PNG:speed/heading/arm-motion vs秒,转身=heading斜坡/跑=speed隆起/拳跳舞=arm尖峰,帮挑窗口)。`LAFAN_segments.ipynb`重构=**4动作×[时间线plot+完整实看+3个填START/END秒的子段cell]**。**下一步**:用户填秒区间挑干净单动作(如"fight 22-25s=右刺拳")→我按区间切段+正确prompt重录重训。完整clip预览用`START=0 END=9999`(cut_motion_window的i1=min(n,...)自动封顶)。
  - **🎯🎯 真正的修复(2026-06-10,三方评审纠正:我一直用错工具修症状)**:**SONIC env自带运动学回放**`run_replay`(`train_agent_trl.py ++replay=True`,`step_replay`逐帧`write_joint_state_to_sim`+`write_root_state_to_sim`把机器人写到参考姿态=**无策略无物理→永不摔**,官方docs就是"Visualize reference motions before training")。**我之前一直用`eval_agent_trl.py`物理WBC跟踪当预览(dance必摔),还狂修症状(play-once/terminations=null)——codex原话"你关了烟雾报警器然后看着房子烧"。根本不需要MimicKit绕路,LeSONIC自带**。新脚本`scripts/gear_sonic_replay_window.sh`(`CLIP=dance START=8 END=12 bash ...`→切robot_filtered窗口→`train_agent_trl.py +exp=manager/universal_token/all_modes/sonic_release ++replay=True ++replay_loop_num=False`运动学回放)。**启动坑**:① train不像eval从ckpt读config→必须`+exp=...`否则`MissingMandatoryValue:algo`;② train的`headless`已在config→用`headless=False`不是`+headless`;③ train init wandb会崩no-tty→`export WANDB_MODE=disabled`;④ `replay_loop_num=False`=播一遍(原生play-once,不用我那SONIC_PLAY_ONCE hack)。smoke验dance窗口Progress 50→75→Replay complete!**不摔** ✅。**两工具分清**:`gear_sonic_replay_window.sh`=看动作选段(运动学不摔);`gear_sonic_preview_window.sh`=物理可跟性筛查(WBC跟不住就摔=该段不可track,是有用信号不是bug)。`LAFAN_segments.ipynb`已全换replay。评审共识:别重训WBC(那是单独研究项)、fight+run先走通、dance/jumps靠严格筛查决定去留、别用MimicKit当唯一不摔预览(LeSONIC自包含)。
  - **🎬 运动学预览(早先MimicKit绕路,已被上面SONIC run_replay取代)**:LeSONIC `LAFAN_segments.ipynb`用**SONIC WBC物理跟踪**(dance/jumps WBC跟不住→摔);MimicKit `view_motion_g1_env`(关电机+FK同步)=**纯运动学回放永不摔**=选段对工具。**仓库边界**:LAFAN clip是MimicKit格式、preview在伞仓→**选段属MimicKit.ipynb(伞仓),训练属LeSONIC**。伞仓新工具:`scripts/cut_mimickit_window.py`(切MimicKit pkl[start_s,end_s]→data/motions/g1/<name>.pkl)+`scripts/mimickit_preview_window.sh`(`CLIP=dance START=8 END=12`→切→`mimickit_preview.sh view`运动学回放)。`mimickit_preview.sh lafan_{fight,run,dance,jumps}`整clip运动学回放(无ckpt=Dummy no policy)。MimicKit.ipynb加§10"LAFAN段挑选"(整clip+窗口cell)。smoke验dance窗口=agent Dummy+alive不摔 ✅。**工作流:MimicKit运动学选段(全clip不摔)→给秒区间→LeSONIC切robot_filtered+录token+训flow3**。flow3=fight+run(WBC跟得住)在LeSONIC的WBC-preview也不摔,dance/jumps只能MimicKit看(WBC要重训=泛化later)。
  - **🔂 play-once(2026-06-10,用户嫌循环看不清时间界限)**:`commands.py:_update_command`加`SONIC_PLAY_ONCE`分支(并入patch0006,在SONIC_NO_REF_RESAMPLE之后):advance后`time_steps=min(time_steps, total-motion_start-1)`=播完夹在末帧hold不loop;R键(`reset_all`已绑)重播。**坑=光夹motion钟不够**,还要关gym两个reset源:`++manager_env.terminations.time_out=null`(motion播完的tracking_time_out)+`++manager_env.config.episode_length_s=600`(默认10s会truncate)。`preview_motion.sh`默认`SONIC_PLAY_ONCE=1 EP_LEN_S=600`(`SONIC_PLAY_ONCE=0`可恢复循环)。smoke验:4s窗口跑25s motion-load只2次(init双载非loop)+进程alive hold末帧=不循环 ✅。
  - **🔂续(完整长clip还循环的二次修)**:短窗口play-once成了,但**完整245s clip还循环**=WBC在难段漂移触发`*_adaptive`终止(`anchor_pos`/`ee_body_pos`是adaptive变体,**忽略静态`threshold=99`override用自算阈值**)→reset→看着像loop。修=preview把5个终止项**整个设null不是抬threshold**:`++manager_env.terminations.{anchor_pos,ee_body_pos,anchor_ori_full,foot_pos_xyz}=null`(term名实测:`time_out`/`anchor_pos`/`anchor_ori_full`/`ee_body_pos`/`foot_pos_xyz`,见terms/*.yaml顶层key)。null term IsaacLab接受(env正常build)。这样无任何终止源(只剩episode 600s>clip)→完整clip播到底+hold不reset。**坑2=`gear_sonic_preview_window.sh`传相对PKL会在错cwd做`-f`检查失败("full clip missing")**→window.sh加`[[ -f $PKL ]]||PKL=$WBC_DIR/$PKL`兜底;notebook完整clip cell去掉多余`PKL=data/...`override只留`CLIP=x START=0 END=9999`。实测完整fight clip跑50s load只2次+alive=播一遍不循环 ✅。**下一步**:用户确认每技能用哪段→按确认的干净段(正确prompt)重录重训;或我按几何重挑候选(真转身=小轨迹大yaw/干净跑=直轨迹小yaw/原地拳=小轨迹+手臂)/重新切割(yaw稳定+低速点切分开转身与直行)。
  - **🎨 viewport默认灯光Stage Lights→Grey Studio**:`manager_env_wrapper.py`(tracked,同C键patch0005)`__init__`GUI分支加`self._apply_viewport_lighting()`→`from omni.kit.viewport.menubar.lighting.actions import _set_lighting_mode; _set_lighting_mode("Grey Studio")`(rig名=`_get_rig_name`对`Grey_Studio.usda`→capwords="Grey Studio")。env `SONIC_VIEWPORT_LIGHT`覆盖(""=留Stage Lights)。best-effort try/except。GUI实测"[viewport] lighting rig set to 'Grey Studio'"无错。
  - **下一步(需用户在场)**:用户跑LAFAN.ipynb逐个看6技能闭环视觉→留好的→flow3(guard枢纽+run转身/慢跑串fight)。MSE好≠闭环视觉好,GUI是真正视觉筛。待commit:`LAFAN.ipynb`+`scripts/{gear_sonic_serve.sh,lafan_segment_skills.py,lafan_mimickit_to_robot_filtered.py}`+`gear_sonic_live.sh`(M,加6skill)+`doc/sonic_lafan_expansion.html`(M)+`.memory`。
- **doc**:`LeSONIC/doc/sonic_lafan_expansion.html`(三模型收敛表+四路排名+阶梯SVG+两闸门+盲点表+第一实验+**§6.5实测结果表(strict vs RELAX+MPJPE_g)**+落地排名;TL;DR/§7已按实测改成"fight GO/jumps NO/接地能腾空不能";codex复审过已改:别说"GR00T只能吐WBC会的token"太绝对/BONES-SEED"零gap"改"风险最低"/63%与PPO标注为外部记录非本仓核实/progress非progress_rate/"30min"限定转好clip后)。
- **关联**:[[mimickit-lafan-fight-training-plan]](LAFAN源+63% run)、[[mimickit-to-vla-distill-plan]](数据侧BC学不出平衡的负面=正是WBC托底的理由)。

## 🔄 StarVLA-PI_v3 能否替换 GR00T 做 SONIC?（2026-06-09 三模型头脑风暴 + codex 复审文档）
**结论：横向移动 (lateral)，不是修复。三方强收敛。** 我们训过 `StarVLA-Qwen3.5-4B-PI_v3-PickOrange`(SO-101,35%)，自然想把同一 PI_v3 训成 SONIC 的 token 生产者替掉 GR00T。接口上 **trivial 能换**（WBC 是干净的可替换边界，注入器照收任何合法 64 维 token），但**修不了已知 3 缺陷里任何一个**，因为真瓶颈在 **head 的离散损失 + obs 无记忆，都与骨干无关**。
- **核心铁律**：StarVLA PI_v3 头**也是 flow-matching 连续回归**(`((pred-velocity)²).mean()`，mimo 翻源码坐实)→ 把 FSQ 离散码当连续回归是**同一原罪**，非 GR00T 独有。flow 多峰性帮不上(每动作只录一条轨迹)。真修 flaw#2 = per-dim CE(对观测 snap 网格值 bin；抽到原生 quantizer 索引则 32 级) 或 pre-quant latent 过 SONIC quantizer —— **GR00T 上一样能做，StarVLA 无特权**。
- **3 缺陷全继承**：①held-out=数据/评测纪律 ②FSQ-连续=同上 ③无记忆单帧=StarVLA 也单帧 obs→chunk，kick/walk 仍需 bootstrap。**反而更差**：StarVLA Bridge/RT-1 base 是 WidowX-style EEF 动作空间与 64 维 token 不匹配，头要 reinit → 迁移红利大半丢失；horizon 16→40 拉伸=回归风险；Qwen3-VL 视觉被地面 ego-cam 浪费(需 vision-off ablation)；7 固定 prompt 用不上 4B 语言。
- **plumbing 坑**：归一化陷阱(q99/minmax 移 bin 边界毁 snap → 必须 identity 或 token/0.5 且对齐逆变换)/action 不是关节(bypass motor-deg 转换/clipping/gripper)/state pathway 都只当前帧/单相机。
- **codex 盲点**：①**非-VLA baseline**(prompt-ID+proprio→MLP→40×64) 若打平=基建非进步 ②串联成功多是注入器+bootstrap 非学到串联 ③**WBC 可行性在 VLA 上游**(RELAX 关的严格跟踪筛查是闸门) ④评测含 chunk 边界不连续度+prompt 切换后头 N 步误差。
- **裁决**：仅当 ①干净 A/B 基线(打平=有用负面结果，堵死"换更好 VLA 就行") 或 ②立刻配 FSQ-aware 损失(修 flaw#2 载体) 才值得。**正确下一步排序(全在 GR00T 上零骨干改动)：先修 flaw#2 离散损失 → 再修 flaw#3 obs history+phase → 然后才谈骨干 A/B。** 例外=未来 scale 到几百多样 prompt 时 Qwen3-VL 语言才值钱。
- **doc**：`LeSONIC/doc/sonic_starvla_swap_brainstorm.html`(三方独立结论表+§3 同一原罪源码+3 缺陷继承表+plumbing 坑表+收益/损失表+盲点+go/no-go 实验+裁决；codex 18 点复审已采纳，裁决复审确认不软化)。关联 [[starvla-so101-cloud-training]][[starvla-vlm-variant-2b-4b-8b]][[sonic-vla-critique-roadmap]]。

## 架构侧 Stage C 要点（评审给的，等路 A 过了再用）
- token 注入：`UniversalTokenModule.decode("g1_dyn",{token_flattened(64),proprioception})` 或 `forward(latent_residual_mode="pre_quantization_replace")`。in-process，免 C++。
- **GR00T 训练目标用 pre-quantization 连续 latent**（非分类、非量化后值），推理时过 SONIC 同一 FSQ quantizer 再 decode（codex+mimo 一致）。
- in-process decode 要复用 SONIC 的 obs 构造 / proprio history / 50Hz+token插值；`sonic_release` 的 `running_mean_std:false`，真正要对齐的是 obs term 布局/history/clipping。
- 14 维手部绕过 WBC（无平衡兜底），要单独处理（mimo 提）。
- 必须三曲线对照：官方 SONIC eval vs external-token playback vs direct-decode，逐帧比动作均值。

关联：[[gear-sonic-preview-setup]]、[[mimickit-to-vla-distill-plan]]（数据侧对照）、[[gr00t-multi-release-env-split]]（N1.7 env）、[[mimickit-lafan-fight-training-plan]]（LAFAN dance/fight 来源）。
